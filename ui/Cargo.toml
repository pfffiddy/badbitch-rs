[package]
name = "llm-desk-ui"
version = "0.2.0"
edition = "2021"
description = "The shared egui interface — identical on desktop and Android"

[dependencies]
llm-desk-core = { path = "../core" }
eframe = "0.29"
serde_json = "1"
