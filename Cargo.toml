[workspace]
resolver = "2"
members = ["core", "ui", "desktop"]
# NOTE: android/rust is intentionally a standalone crate (own workspace) so
# its Android-only eframe features don't unify into the desktop build.

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 2
